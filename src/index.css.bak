/* ---------------------------
   Reset y layout base
--------------------------- */
*,
*::before,
*::after { box-sizing: border-box; }

html, body {
  margin: 0;
  height: 100%;
  overscroll-behavior: none;
  -webkit-text-size-adjust: 100%;
}

body { overflow: hidden; background:#fff; }

#root {
  position: fixed;
  inset: 0;
  display: flex;
}

.App {
  flex: 1;
  width: 100vw;
  min-height: 100svh;
  height: -webkit-fill-available;
  display: flex;
  flex-direction: column;
  overflow: hidden;
  padding-left: env(safe-area-inset-left);
  padding-right: env(safe-area-inset-right);
  padding-bottom: env(safe-area-inset-bottom);
}

.app-header {
  width: 100%;
  box-sizing: border-box;
  flex: 0 0 auto;
}

.scroll-area {
  flex: 1 1 auto;
  overflow: auto;
  -webkit-overflow-scrolling: touch;
}

/* ---------------------------
   Tokens de color y radius
--------------------------- */
:root {
  --bg: #0f172a; 
  --card: #ffffff;
  --text: #0b1220;
  --muted: #6b7280;
  --primary: #0b5ed7;
  --radius: 14px;
  --shadow: 0 10px 30px rgba(2,6,23,.18);
}

/* ---------------------------
   Modal reutilizable
--------------------------- */
.ui-backdrop {
  position: fixed;
  inset: 0;
  background: rgba(2,6,23,.56);
  display: grid;
  place-items: center;
  z-index: 60;
}

.ui-modal {
  width: min(520px,92vw);
  background: var(--card);
  color: var(--text);
  border-radius: var(--radius);
  box-shadow: var(--shadow);
  padding: 20px 22px;
}

.ui-modal__title {
  margin: 0 0 12px;
  font-size: 1.1rem;
  font-weight: 700;
}

.ui-field { display: grid; gap: 6px; margin: 10px 0 18px; }
.ui-label { font-size: .9rem; color: var(--muted); }

.ui-input {
  border: 1px solid #e5e7eb;
  border-radius: 10px;
  padding: 10px 12px;
  font-size: 1rem;
}

.ui-actions {
  display: flex;
  gap: 10px;
  justify-content: flex-end;
}

/* ---------------------------
   Botones globales
--------------------------- */
.btn {
  border: 0;
  border-radius: 12px;
  padding: 10px 16px;
  font-weight: 600;
  cursor: pointer;
}

.btn--primary { background: var(--primary); color:#fff; }
.btn--ghost   { background: transparent; color: var(--text); border:1px solid #e5e7eb; }
/* Tipografía base más agradable */
html, body { font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji","Segoe UI Emoji"; }

/* Modal: jerarquía y aire */
.ui-modal { padding: 24px 26px; border-radius: 16px; }
.ui-modal__title { font-size: 1.25rem; font-weight: 800; letter-spacing: .2px; margin-bottom: 14px; }
.ui-label { font-size: .95rem; font-weight: 600; color: #111827; }

/* Input más cómodo y con focus claro */
.ui-input{
  border:1px solid #d1d5db;
  border-radius: 12px;
  padding: 12px 14px;
  font-size: 1rem;
  outline: none;
  transition: box-shadow .15s, border-color .15s;
}
.ui-input:focus{
  border-color:#2563eb;
  box-shadow: 0 0 0 4px rgba(37,99,235,.15);
}

/* Botones: primario ancho, secundario silencioso */
.ui-actions { gap: 12px; }
.btn { border-radius: 14px; padding: 12px 16px; font-weight: 700; }
.btn--primary { min-width: 220px; }
.btn--ghost { background:#f3f4f6; color:#111827; border:1px solid #e5e7eb; }
.btn--ghost:hover { background:#e5e7eb; }
