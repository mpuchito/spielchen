/* Bloqueo total de scroll + iPhone full screen */
html, body {
  margin: 0;
  padding: 0;
  height: 100%;
  overscroll-behavior: none;
  -webkit-text-size-adjust: 100%;
}

body {
  position: fixed;       /* fija el documento al viewport */
  inset: 0;
  width: 100%;
  overflow: hidden;      /* sin scroll global */
  background: #fff;
}

#root, .App {
  position: absolute;
  inset: 0;
  width: 100%;
  min-height: 100vh;     /* fallback */
  min-height: 100svh;    /* iOS/Android modernos */
}

/* iOS safe area */
.App {
  padding-left: env(safe-area-inset-left);
  padding-right: env(safe-area-inset-right);
  padding-bottom: env(safe-area-inset-bottom);
}

/* Si necesitas scroll interno, úsalo explícito */
.scroll-area {
  flex: 1 1 auto;
  overflow: auto;
  -webkit-overflow-scrolling: touch;
}
